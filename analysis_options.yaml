# This file configures the static analysis results for your project (errors,
# warnings, and lints).
#
# This enables the 'recommended' set of lints from `package:lints`.
# This set helps identify many issues that may lead to problems when running
# or consuming Dart code, and enforces writing Dart using a single, idiomatic
# style and format.
#
# If you want a smaller set of lints you can change this to specify
# 'package:lints/core.yaml'. These are just the most critical lints
# (the recommended set includes the core lints).
# The core lints are also what is used by pub.dev for scoring packages.

include: package:lints/recommended.yaml

analyzer:
  plugins:
    - custom_lint

custom_lint:
  rules:
    # ── Architecture ───────────────────────────────────────────────────────
    # render() must be pure — no data transformations inside
    - no_logic_in_render: true

    # Components created inline lose identity on every morph
    - no_inline_component_creation: true

    # Components stored as local variables lose identity on every call
    - components_must_be_fields: true

    # morph() inside onInput re-renders on every keystroke
    - no_morph_in_on_input: true

    # ── ElementBuilder usage ───────────────────────────────────────────────
    # Void elements (Input, Img, Br, etc.) cannot receive children
    - void_element_with_children: true

    # Every ElementBuilder must be finalized with call() to produce a Morphic node
    - element_builder_missing_call: true
